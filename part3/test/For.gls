LET For_shadowing BE
	$ i = 0: $ Error if not commented
	FOR {INCREASE i FROM 0 TO 10 STEP 1} REPEAT
		OUT(i):
	END:
	OUT(i):
	$ j = 10: $ Error if not commented
	FOR {DECREASE j FROM 10 TO 0 STEP -1} REPEAT
		OUT(j):
	END:
	OUT(j):
END